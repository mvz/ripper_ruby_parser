# Samples that need Ruby 2.7 or higher

# Beginless ranges
..1
..foo

# Argument forwarding
def foo(...)
  bar(...)
  bar(qux, ...)
end

# One-line pattern matching (experimental)
1 in foo
2 in bar => baz
