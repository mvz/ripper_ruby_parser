language: ruby
rvm:
  - 2.2
  - 2.3
  - 2.4
  - 2.5
  - ruby-head
install:
  - bundle install --without local_development
matrix:
  allow_failures:
    - rvm: ruby-head
branches:
  only:
    - master
